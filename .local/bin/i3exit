#!/bin/bash
# ~/.local/bin/i3exit

lock() {
  i3lock \
    --nofork \
    --color=1d2021e6 \
    --inside-color=00000022 \
    --ring-color=458588e6 \
    --keyhl-color=a89984e6 \
    --bshl-color=d79921e6 \
    --separator-color=458588e6 \
    --insidever-color=00000022 \
    --ringver-color=458588e6 \
    --verif-color=b16286e6 \
    --insidewrong-color=00000022 \
    --ringwrong-color=cc241de6 \
    --wrong-color=cc241de6 \
    --time-color=d79921e6 \
    --date-color=d79921e6 \
    --blur 9 \
    --clock
}

case "$1" in
  lock)
    lock ;;
  logout)
    i3-msg exit ;;
  suspend)
    lock && systemctl suspend ;;
  hibernate)
    lock && systemctl hibernate ;;
  reboot)
    systemctl reboot ;;
  poweroff)
    systemctl poweroff ;;
  *)
    echo "Usage: $0 {lock|logout|suspend|hibernate|reboot|poweroff}"
    exit 1
    ;;
esac
